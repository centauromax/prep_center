{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "bash -c 'python backend/manage.py collectstatic --noinput --clear && gunicorn --chdir backend prep_center.wsgi:application'"
  }
}
