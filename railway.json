{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "mkdir -p /app/staticfiles frontend/fbasaving/static && cp -r backend/fbasaving/static/* frontend/fbasaving/static/ && cd backend && python -m pip install -r requirements.txt && python manage.py collectstatic --noinput --clear"
  },
  "deploy": {
    "startCommand": "cd backend && gunicorn prep_center.wsgi:application"
  }
} 
