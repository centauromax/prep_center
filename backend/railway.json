{
  "build": {
    "env": {
      "PYTHON_VERSION": "3.12"
    }
  },
  "deploy": {
    "startCommand": "python manage.py migrate && python manage.py populate_clienti && python manage.py populate_ean && gunicorn prep_center.wsgi"
  }
} 