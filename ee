# 1. Crea le nuove cartelle backend e frontend
mkdir backend frontend
# 2. Sposta tutti i file e le cartelle attuali (tranne il workspace file)
#    nella nuova cartella 'backend'.
#    Elenco esplicito basato sulla tua struttura:
mv bin db.sqlite3 fbasaving locale manage.py media prep_center prep_management requirements.txt return_management settings.py sftp.json shell static style.css sync_translations.py temp_script templates backend/
# NOTA: Questo sposta la cartella 'prep_center' originale (con settings.py, venv, etc.)
#       dentro 'backend'. Sposta anche i file potenzialmente fuori posto
#       (settings.py, style.css, shell) in 'backend' per ora.
# 3. Rimuovi i file potenzialmente errati dalla cartella di configurazione (ora in backend)
#    Assumendo che i nomi siano letteralmente 'Error' e 'Files':
rm backend/prep_center/Error backend/prep_center/Files
# 4. Crea un file placeholder nella cartella frontend
touch frontend/README.md
# 5. Crea il file .gitignore (se non esiste già)
touch .gitignore
echo OK!
